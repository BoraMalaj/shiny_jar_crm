[phases.setup]
cmds = ["pip install poetry"]

[phases.install]
cmds = ["poetry install --no-dev"]

[phases.build]
cmds = ["echo 'Building...'"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"