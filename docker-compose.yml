version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"  # or whatever port your app uses
    environment:
      DATABASE_URL: ${DATABASE_URL}
      # Remove DB_HOST, DB_USER, etc - just use DATABASE_URL